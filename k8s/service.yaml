apiVersion: v1
kind: Service
metadata:
  name: rag-email-mvp
  labels:
    app: rag-email-mvp
spec:
  type: ClusterIP
  selector:
    app: rag-email-mvp
  ports:
    - name: http
      port: 80
      targetPort: 8000
